type Events @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  date: AWSDate!
  description: String!
}

type Users @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  profilePic: String
}
